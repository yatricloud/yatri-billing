{
  "name": "yatri-billing-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i tailwind-input.css -o tailwind.css --minify",
    "watch:css": "tailwindcss -i tailwind-input.css -o tailwind.css --watch",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "tailwindcss": "^3.4.0"
  }
}
